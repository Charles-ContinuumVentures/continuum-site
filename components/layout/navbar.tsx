"use client";
import { useState } from "react";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { BrandMark } from "./brand-mark";
import { Container } from "./container";
const links = [{ label: "Home", href: "/" }, { label: "Our Philosophy", href: "/philosophy" }, { label: "Ventures", href: "/ventures" }, { label: "Insights", href: "/insights" }, { label: "Contact", href: "/contact" }];
export function Navbar() { const [open, setOpen] = useState(false); return <header className="relative z-50 border-b border-stone-200/80 bg-white/90 backdrop-blur-xl"><Container className="flex h-20 items-center justify-between"><BrandMark/><nav className="hidden items-center gap-8 lg:flex" aria-label="Primary navigation">{links.map((link) => <Link key={link.href} href={link.href} className="rounded-sm text-sm font-medium text-stone-600 hover:text-stone-950 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-emerald-600">{link.label}</Link>)}</nav><Link href="/contact" className="hidden min-h-11 items-center rounded-full bg-stone-950 px-5 text-sm font-semibold text-white hover:bg-emerald-800 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-emerald-600 lg:inline-flex">Start a Conversation</Link><button type="button" className="grid size-11 place-items-center rounded-full border border-stone-200 focus-visible:outline-2 focus-visible:outline-emerald-600 lg:hidden" aria-expanded={open} aria-controls="mobile-navigation" aria-label={open ? "Close navigation menu" : "Open navigation menu"} onClick={() => setOpen(!open)}>{open ? <X className="size-5" aria-hidden="true"/> : <Menu className="size-5" aria-hidden="true"/>}</button></Container><div id="mobile-navigation" hidden={!open} className="absolute inset-x-0 top-full border-b border-stone-200 bg-white shadow-xl lg:hidden"><Container className="py-5"><nav className="flex flex-col" aria-label="Mobile navigation">{links.map((link) => <Link key={link.href} href={link.href} onClick={() => setOpen(false)} className="rounded-md px-3 py-3 text-base font-medium text-stone-700 hover:bg-stone-50 focus-visible:outline-2 focus-visible:outline-emerald-600">{link.label}</Link>)}<Link href="/contact" onClick={() => setOpen(false)} className="mt-3 flex min-h-12 items-center justify-center rounded-full bg-stone-950 px-5 text-sm font-semibold text-white">Start a Conversation</Link></nav></Container></div></header>; }
